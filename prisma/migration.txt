/*
  # 직접 쿼리 실행 (로컬은 --remote제거해야 함.)
  npx wrangler d1 execute destiny-local --command "ALTER TABLE CelebrityTranslation ADD COLUMN aiResponse TEXT;"
  npx wrangler d1 execute destiny-new --remote --command "ALTER TABLE CelebrityTranslation ADD COLUMN aiResponse TEXT;"

  # 쿼리 직접실행
  npx prisma generate    (동기화)
  npx prisma migrate dev (마이그레이션 쿼리 생성)
  npx wrangler d1 execute destiny-local --local --file=prisma/migrations/20250805102303_/migration.sql
*/